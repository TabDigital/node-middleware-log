{
  "name": "node-middleware-log",
  "version": "1.0.0",
  "description": "Ndjson logging middleware",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "TabDigital/node-middleware-log",
  "keywords": [
    "log",
    "middleware",
    "ndjson"
  ],
  "license": "MIT",
  "dependencies": {
    "emit-response-size": "^1.0.0",
    "http-ndjson": "^2.3.3",
    "is-stream": "^1.0.1"
  },
  "devDependencies": {
    "concat-stream": "^1.5.1",
    "dependency-check": "^2.5.1",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.4.0",
    "standard": "^5.3.1",
    "tape": "^4.2.2"
  },
  "files": [
    "index.js",
    "bin/*"
  ]
}
